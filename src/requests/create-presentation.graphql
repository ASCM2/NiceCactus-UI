mutation createPresentation($user: ID!, $business: ID!, $logo: Upload, $description: String!) {
  createpresentation(user: $user, business: $business, presentation: { logo: $logo, description: $description }) {
    id
    logo {
      id
      name
      contentType
      src
    }
    description
  }
}
