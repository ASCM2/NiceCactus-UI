query queryBusiness($user: ID!, $business: ID!) {
  business(user: $user, id: $business) {
    id
    image {
      src
    }
    category
    shortname
    longname
    images {
      id
      src
      liked
      likes
    }
    follower
    followersNumber
    liked
    likes
    owner
  }
}
