query searchBusinesses($user: ID!, $term: String!, $after: ID, $number: Int!, $categories: [AllowedCategories!]) {
  search(user: $user, term: $term, after: $after, number: $number, categories: $categories) {
    term
    last
    end
    businesses {
      id
      category
      shortname
      smalldescription
      address
      postalCode
      city
      country
      location {
        lat
        lng
      }
      follower
      followersNumber
      likes
    	owner
    }
  }
}
