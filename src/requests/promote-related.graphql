mutation promoteRelated($user: ID!, $business: ID!, $related: ID!) {
  promoterelated(user: $user, business: $business, related: $related) {
    id
    related(user: $user) {
      id
      image {
        src
      }
      category
      promoted
      shortname
      smalldescription
    }
  }
}
