query lastBusinesses($user: ID!, $after: ID, $number: Int!, $categories: [AllowedCategories!]) {
  last(user: $user, after: $after, number: $number, categories: $categories) {
    last
    end
    businesses {
      id
      category
      shortname
      smalldescription
      address
      postalCode
      city
      country
      location {
        lat
        lng
      }
      follower
      followersNumber
      likes
    	owner
    }
  }
}
